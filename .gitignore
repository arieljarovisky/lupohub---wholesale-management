node_modules
backend/dist
frontend/dist
.env
.DS_Store
# Certificados y claves AFIP (no subir a Git; la carpeta y su README sí se versionan)
backend/afip-certs/*.crt
backend/afip-certs/*.key
backend/afip-certs/*.pem

# Documentación .md (no subir a Git)
*.md
docs/